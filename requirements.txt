# НЕЙРОНОВО · Говорящие головы — зависимости (PyTorch-вариант движка Ditto).
#
# ВАЖНО: PyTorch ставится ОТДЕЛЬНО скриптом install (нужен индекс CUDA), напр.:
#   pip install torch==2.5.1 torchaudio==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
# См. install.bat / install.sh.
#
# pyximport (внутри Ditto) компилирует .pyx при первом запуске — нужен Python и C-компилятор
# (Windows: «Build Tools for Visual Studio»; Linux: gcc/g++; macOS: Xcode Command Line Tools).

gradio>=6
huggingface_hub>=0.25

numpy>=1.26,<2.1
scipy>=1.11
scikit-image>=0.22
scikit-learn>=1.3

librosa>=0.10.2
soundfile>=0.12
soxr>=0.5
audioread>=3.0
numba>=0.59
llvmlite>=0.42

opencv-python-headless>=4.9
imageio>=2.34
imageio-ffmpeg>=0.5
tifffile>=2024.1
pillow>=10.0

einops>=0.7
mediapipe>=0.10.14
onnxruntime-gpu>=1.17
cython>=3.0
filetype>=1.2
tqdm>=4.66
pyyaml>=6.0
